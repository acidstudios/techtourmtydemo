<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:local="clr-namespace:TechTourDemo" 
	x:Class="TechTourDemo.TechTourDemoPage" 
	Title="X.F Demo!">
	<ContentView.ToolbarItems>
		<ToolbarItem Name="Agregar" Order="Primary" Text="Agregar" Clicked="Handle_Clicked" />
	</ContentView.ToolbarItems>
	<ListView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" x:Name="TodoList" ItemSelected="Handle_ItemSelected">
		<ListView.ItemTemplate>
			<DataTemplate>
				<TextCell Text="{Binding Name}" />
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>
