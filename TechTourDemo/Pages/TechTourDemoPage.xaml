<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:local="clr-namespace:TechTourDemo" 
	x:Class="TechTourDemo.TechTourDemoPage" 
	Title="X.F Demo!"
	x:Name="MainPage">
	<ContentView.ToolbarItems>
		<ToolbarItem Name="Agregar" Order="Primary" Text="Agregar" Command="{Binding AddTodoCommand}" />
	</ContentView.ToolbarItems>
	<ListView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" x:Name="TodoList" ItemsSource="{Binding TodoItems}">
		<ListView.ItemTemplate>
			<DataTemplate>
				<TextCell Text="{Binding Name}" Command="{Binding BindingContext.SelectTodoItemCommand, Source={x:Reference MainPage}}" CommandParameter="{Binding .}" />
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>
